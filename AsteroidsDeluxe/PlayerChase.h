#pragma once
#include "PolycodeView.h"
#include <memory>

class PlayerChase
{
public:
	PlayerChase(void);

protected:
	float AimAtTarget(Vector3 origin, Vector3 target, float rotation);

private:

};

